/*
    enum DisplayPanelID

    Compiler-checked labels for the DisplayPanels so that the
    TileGameDisplayPanel (TILE_GAME) can react to specific panels being
    shown/hidden.

 */
public enum DisplayPanelID {
    TILE_GAME,
    GAME,
    MENU,
    HELP,
    INFO,
}
